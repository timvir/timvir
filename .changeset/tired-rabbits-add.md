---
"timvir": patch
---

Override text color when forcing light or dark theme

Forcing a theme now sets both the background image (checkered background) and also text color.
This ensures that elements that rely on currentColor remain readable.
