# FontFamilyMatrix

Use the `<FontFamilyMatrix>` component to show which font faces are used. The font
faces are arranged in a matrix with two columns (one for normal and italic styles,
respectively), and any number of rows (which represent the weights).

<Sample variant="basic" />
