import { Viewport } from "../../Viewport";

# Footer

The `<Footer>` element is an optional element which can be placed at the very
bottom of pages. It contains links (internal or external), as well as the copyright
notice.

<Viewport src="/docs/components/Footer/samples/basic" />
