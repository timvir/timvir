import Wrapper from "@/timvir/app-page-wrapper";
export default Wrapper;

# @timvir/std/base58

<div
  style={{
    fontSize: "1.1em",
    margin: "24px -16px .5em",
    padding: "6px 16px 3px",
    background: "var(--c-p-0)",
    borderBottom: "2px solid var(--c-p-2)",
  }}
>
  <span style={{ fontWeight: 500 }}>encode</span> <span style={{ color: "var(--timvir-secondary-text-color)" }}>:: (input: string) => string</span>
</div>

<div
  style={{
    fontSize: "1.1em",
    margin: "24px -16px .5em",
    padding: "6px 16px 3px",
    background: "var(--c-p-0)",
    borderBottom: "2px solid var(--c-p-2)",
  }}
>
  <span style={{ fontWeight: 500 }}>decode</span> <span style={{ color: "var(--timvir-secondary-text-color)" }}>:: (input: string) => string</span>
</div>
