# API

<div style={ { display: "grid", gridTemplateColumns: "min-content 1fr" } }>
  <div style={ { fontWeight: "bold", marginRight: 16 } }>Root:</div>
  <div>{`<div>`}</div>
  <div style={ { fontWeight: "bold", marginRight: 16 } }>Ref:</div>
  <div>Forward <span style={ { color: "rgb(153, 153, 153)" } }>(HTMLDivElement)</span></div>
</div>

## Props

<Props {...{"fields":[{"name":"location","type":{"optional":false,"name":"{ asPath: string; push: (path: string) => void; }","module":""},"comment":{"shortText":""}},{"name":"toc","type":{"optional":false,"name":"readonly Node[]","module":""},"comment":{"shortText":""}},{"name":"Link","type":{"optional":false,"name":"typeof Link","module":""},"comment":{"shortText":""}},{"name":"mdxComponents","type":{"optional":true,"name":"MDXProviderComponents","module":""},"comment":{"shortText":"Overrides the built-in MDX component implementations.\n\nTimvir only provides styling for Markdown components, no interactivity or customization.\nThis is done to keep the core small. For example, code blocks do not provide syntax\nhighlighting. If you want to enable syntax highlighting in code blocks, use the\n'<Code>' component from '@timvir/blocks'."}},{"name":"search","type":{"optional":true,"name":"{ label?: ReactNode; Component: ComponentType<{ open: boolean; }>; }","module":""},"comment":{"shortText":"Search Configuration. When provided, then the Search menu will appear in the sidebar."}},{"name":"Footer","type":{"optional":true,"name":"ElementType<{}>","module":""},"comment":{"shortText":"Optional footer which is shown at the bottom of every page.\n\nYou can use any component here, though timvir provides a ready-made component which\nshould fit most use cases ('<Footer>')."}}]}} />

export const required = (
  <svg
    x="0px"
    y="0px"
    width="10px"
    height="10px"
    viewBox="0 0 16 16"
    style={ { position: "relative", top: 0, display: "inline-block", marginRight: 8 } }
  >
    <g transform="translate(0, 0)">
      <circle cx="8" cy="8" r="8" fill="#444444"></circle>
    </g>
  </svg>
);

export const optional = (
  <svg
    x="0px"
    y="0px"
    width="10px"
    height="10px"
    viewBox="0 0 16 16"
    style={ { position: "relative", top: 0, display: "inline-block", marginRight: 8 } }
  >
    <g transform="translate(0, 0)">
      <circle
        cx="8"
        cy="8"
        r="7.5"
        fill="none"
        stroke="#444444"
        strokeLinecap="round"
        strokeLinejoin="round"
        data-cap="butt"
      ></circle>
    </g>
  </svg>
);

export const Props = ({ fields }) => (
  <div style={ { marginTop: 16, display: "grid", gridGap: 24 } }>
    {fields.map(({ name, type, def, example, comment }) => (
      <div key={name}>
        <div style={ { marginLeft: -18 } }>
          {type.optional ? optional : required}
          <span style={ { fontWeight: "bold" } }>{name}</span>{`: `}
          <span>{type.name}</span>
          {def && (
            <>
             {` = `}<span style={ { fontWeight: "bold" } }>{def}</span>
            </>
          )}
        </div>
        <div>{comment.shortText}</div>
        {example && (
          <div style={ { marginLeft: 0, marginTop: 8, color: "rgb(153, 153, 153)", fontSize: "0.8rem" } }>
            Example: <span style={ { fontWeight: "bold" } }>{example}</span>
          </div>
        )}
      </div>
    ))}
  </div>
)
