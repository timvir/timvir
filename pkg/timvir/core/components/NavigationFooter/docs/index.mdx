import { Viewport } from "timvir/blocks";

# NavigationFooter

The `<NavigationFooter>` component allows users to navigate to the previous and next page.
It is automatically inserted into every page.

## Sample

<Viewport src="/docs/components/NavigationFooter/samples/basic" />
